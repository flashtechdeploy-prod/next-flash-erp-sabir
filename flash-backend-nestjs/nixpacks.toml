[install]
pkgManager = "npm"

[build]
cmds = [
  "export NODE_ENV=development && npm run build"
]

[start]
cmd = "npm run start:prod"

[env]
NODE_ENV = "production"

[processes]
web = "npm run start:prod"
