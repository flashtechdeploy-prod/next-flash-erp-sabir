[install]
pkgManager = "npm"

[build]
cmds = [
  "npm install",
  "npm run build",
  "test -d dist || (echo 'Build failed: dist folder missing' && exit 1)"
]

[start]
cmd = "npm run start:prod"

[env]
NODE_ENV = "production"

[processes]
web = "npm run start:prod"
