[install]
pkgManager = "npm"

[build]
cmds = [
  "npm run build",
  "ls -la dist || (echo 'ERROR: dist folder missing!' && exit 1)"
]

[start]
cmd = "npm run start:prod"

[processes]
web = "npm run start:prod"
